# i3 config file (v4)
exec_always --no-startup-id xrdb -load ~/.cache/wal/colors.Xresources

# load colors from xresources
set_from_resource $foreground i3wm.foreground #C0C5CE
set_from_resource $background i3wm.background #1B2B34
set_from_resource $color0 i3wm.color0 #1B2B34
set_from_resource $color1 i3wm.color1 #EC5f67
set_from_resource $color2 i3wm.color2 #99C794
set_from_resource $color3 i3wm.color3 #FAC863
set_from_resource $color4 i3wm.color4 #6699CC
set_from_resource $color5 i3wm.color5 #C594C5
set_from_resource $color6 i3wm.color6 #5FB3B3
set_from_resource $color7 i3wm.color7 #C0C5CE
set_from_resource $color8 i3wm.color8 #65737E
set_from_resource $color9 i3wm.color9 #EC5f67
set_from_resource $color10 i3wm.color10 #99C794
set_from_resource $color11 i3wm.color11 #FAC863
set_from_resource $color12 i3wm.color12 #6699CC
set_from_resource $color13 i3wm.color13 #C594C5
set_from_resource $color14 i3wm.color14 #5FB3B3
set_from_resource $color15 i3wm.color15 #D8DEE9
set $transparent #00000000
set $semitransparent #00000066

set $mod Mod4
set $terminal WINIT_X11_SCALE_FACTOR=1 alacritty
set $launcher rofi
set $background_image ~/ownCloud/Wallpapers/wallhaven-497395.png

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Iosevka,FontAwesome 11

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
# exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status pkill -RTMIN+1 i3blocks
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use light to control display brightness
bindsym XF86MonBrightnessUp exec light -A 5
bindsym XF86MonBrightnessDown exec light -U 5

# show calculator
bindsym XF86Calculator exec gnome-calculator

# hide/show bars and notification (presenting mode)
bindsym $mod+Delete exec polybar-msg cmd hide && notify-send DUNST_COMMAND_PAUSE
bindsym $mod+Insert exec polybar-msg cmd show && notify-send DUNST_COMMAND_RESUME

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec $terminal

# kill focused window
bindsym $mod+Shift+q kill

# start launcher
# bindsym Alt+Space exec rofi -show drun

# take a screenshot
bindsym $mod+Shift+s exec flameshot gui

# change focus
focus_follows_mouse no
focus_on_window_activation focus

bindsym $mod+j focus left
bindsym $mod+k focus down
# bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle; [floating] border normal 1; [tiling] border pixel 1

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

bindsym $mod+Next workspace next_on_output
bindsym $mod+Prior workspace prev_on_output

# scratchpad handling
bindsym $mod+BackSpace move scratchpad
bindsym $mod+Shift+BackSpace scratchpad show

bindsym $mod+l exec i3lock -c 000000

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $wsTerm "1:"
set $wsWeb "2:"
set $wsMail "3:"
set $wsCode "4:"
set $wsChat "5:"
set $wsDesign "6:"
set $wsScreen "7:"
set $wsDoc "8:"
set $wsMusic "9:"

# switch to workspace
bindsym $mod+1 workspace number $wsTerm
bindsym $mod+2 workspace number $wsWeb
bindsym $mod+3 workspace number $wsMail
bindsym $mod+4 workspace number $wsCode
bindsym $mod+5 workspace number $wsChat
bindsym $mod+6 workspace number $wsDesign
bindsym $mod+7 workspace number $wsScreen
bindsym $mod+8 workspace number $wsDoc
bindsym $mod+9 workspace number $wsMusic

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $wsTerm
bindsym $mod+Shift+2 move container to workspace number $wsWeb
bindsym $mod+Shift+3 move container to workspace number $wsMail
bindsym $mod+Shift+4 move container to workspace number $wsCode
bindsym $mod+Shift+5 move container to workspace number $wsChat
bindsym $mod+Shift+6 move container to workspace number $wsDesign
bindsym $mod+Shift+7 move container to workspace number $wsScreen
bindsym $mod+Shift+8 move container to workspace number $wsDoc
bindsym $mod+Shift+9 move container to workspace number $wsMusic

# map applications to workspaces
# browser
assign [class="Firefox"] workspace $wsWeb
assign [class="qutebrowser"] workspace $wsWeb
# mail
assign [class="Google-chrome"] workspace $wsMail
assign [class="Thunderbird"] workspace $wsMail
# code
assign [class="^Code$"] workspace $wsCode
# chat
assign [class="Microsoft Teams"] workspace $wsChat
# design
assign [title="yEd"] workspace $wsDesign
# virtual machine
assign [class="VirtualBox"] workspace $wsScreen
# music
assign [class="Spotify"] workspace $wsMusic

# float video window
for_window [class="mpv"] floating enable
for_window [class="Vlc"] floating enable
for_window [class="Gnome-calculator"] floating enable
for_window [class="termlauncher"] floating enable

bindsym $mod+Tab exec rofi -modi "window#drun#run" -show window
bindsym $mod+n exec cat ~/.config/i3/desktops | $launcher -p "Switch to workspace" -dmenu -columns 3 -lines 4 | xargs i3-msg workspace
bindsym $mod+m exec cat ~/.config/i3/desktops | $launcher -p "Move window to workspace" -dmenu -columns 3 -lines 4 | xargs i3-msg move window to workspace
bindsym $mod+b exec xrandr | grep -i " connected" | awk '{ print $1}' | rofi -p "Move workspace to output" -dmenu -no-custom | xargs -L1 i3-msg move workspace to
bindsym $mod+o exec ~/bin/termlauncher.sh ~/bin/fzf_finder.sh

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

default_border pixel 1
for_window[floating] border normal 1
gaps inner 4
gaps outer 2
smart_gaps on

title_align center
show_marks yes 

#class                     border              backgr.         text            indicator       child_border
client.focused             $foreground         $color8         $foreground     $foreground     $foreground
client.focused_inactive    $background         $background     $foreground     $foreground     $background
client.unfocused           $background         $background     $color8         $foreground     $background
client.urgent              $color8             $background     $color1         $foreground     $foreground
client.placeholder         $background         $background     $foreground     $background     $color8
client.background          $semitransparent

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#     i3bar_command i3bar --transparency
#     position top
#     strip_workspace_numbers yes
#     font pango:JetBrainsMono,FontAwesome 10.5
#     status_command i3blocks
#     colors {
#         background #00000000
#     }
# }
exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# compositor
exec --no-startup-id picom -b

# background
exec --no-startup-id feh --bg-fill $background_image

# notification daemon
exec --no-startup-id dunst &

# startup apps
exec --no-startup-id owncloud &

# setup keyboard layouts
exec_always --no-startup-id "setxkbmap -layout us,gr -variant ,extended -option grp:alt_shift_toggle"

# enable tapping and increase acceleration for the touchpad
exec_always --no-startup-id "xinput --set-prop 'Synaptics TM3471-020' 'libinput Tapping Enabled' 1"
exec_always --no-startup-id "xinput --set-prop 'Synaptics TM3471-020' 'libinput Accel Speed' '0.5'"

exec --no-startup-id i3-msg 'workspace $wsTerm'

